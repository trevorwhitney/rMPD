# #player = new LocalPlayback()

# #LocalPlayback =
#   #playback = document.getElementById("audio_player")

#   play = ->
#     playback.volume(1)
#     playback.play()

#   stop = ->
#     playback.stop()

#   source = (source)->
#     playback.src(source)

#   reset = ->
#     playback.stop()
#     playback.play()
